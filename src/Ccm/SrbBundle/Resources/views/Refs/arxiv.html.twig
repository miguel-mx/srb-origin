{% extends 'CcmSrbBundle::layout.html.twig' %}

{% block menu %}
{{ knp_menu_render('CcmSrbBundle:Builder:mainMenu') }}
{% endblock %}
{% set option =  'Importar desde ArXiv'  %}
	
{% block col3 %}
<div class="ym-gbox-right">
  <div class="ym-grid">
    <!-- 2 col margen a la derecha -->
    <div style="margin-left: 160px;"><br />

        <div style="margin-top: 10px;">
            <h1>Importación desde Arxiv</h1>
            <br>
        </div>

    <form action="{{ path('arxiv_post') }}" method="post" {{ form_enctype(form) }}>

        {{ form_errors(form) }}

        <div style="color: red; font-weight: bold;">{{ form_errors(form.idlist) }}</div>
        <div><b>ArXive Id's:</b><br /><br /></div>
        <div>{{ form_widget(form.idlist, { 'attr': {'size': '35%'} }) }}</div>

        <!--{{ form_widget(form) }}-->
        {{ form_rest(form) }}

        <button type="submit" class="ym-button ym-upload" />Importar referencia</button>
    </form>

    </div>
    <!-- margin -->

  </div>
  <!-- grid -->
</div>
<!-- block col3 -->
{% endblock %}

{% block menuL %}
        <ul>
    	    <li> <a href="{{ path('arxiv') }}"> Desde Archivo </a> </li>
        </ul>

	<ul class="record_actions">
    	<li><a href="{{ path('refs') }}"> Regresar </a></li>
	</ul>
{% endblock %}

